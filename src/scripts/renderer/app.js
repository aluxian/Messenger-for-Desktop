// Remove the app and 'Electron' from the user agent
// return userAgent
//   .replace(new RegExp(this.manifest.productName + '/[\\S]*', 'g'), '')
//   .replace(new RegExp('Electron/[\\S]*', 'g'), '')
//   .replace(/[ ]+/g, ' ');
